# Hannah Burns Portfolio

Static portfolio site. No build step required.

## GitHub Pages
1. Create a new public repository (for example `hannah-portfolio`).
2. Upload `index.html`, `styles.css`, `script.js`, and the `assets` folder.
3. Open repository **Settings → Pages**.
4. Under **Build and deployment**, choose **Deploy from a branch**.
5. Choose `main` and `/ (root)`, then Save.

## One remaining edit
The exact LinkedIn profile URL was not available. In `index.html`, search for `LinkedIn • URL needed` and replace that span with an anchor when the URL is known.
